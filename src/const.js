const FilterType = {
  EVERYTHING: 'everything',
  FUTURE: 'future',
  PRESENT: 'present',
  PAST: 'past'
};

const SortType = {
  DEFAULT: 'default',
  TIME: 'time',
  PRICE: 'price',
  DISABLED: 'disabled'
};

export { FilterType, SortType };
